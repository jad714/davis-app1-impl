/*
 *  UCF COP3330 Fall 2021 Application Assignment 1 Solution
 *  Copyright 2021 Joshua Davis
 */

package app;

public class ParseTyping {

    public boolean enforceDateFormat(){
        // The "Add Item to List" button calls this to enforce that the date was typed correctly.
        // Parses the string according to the required format.
        // If String is in the correct format (YYYY-MM-DD), return true, else return false.
        // This will be one of several methods that can cause the rejection of "Add Item".
        // The below variable is temporary and only for getting the program to execute.
        boolean temp = false;
        return false;
    }

    public boolean enforceTitleCharCount(){
        // The List name must be at least 3 characters long, so this will ensure that it is.
        // If the char count is incorrect, return false.
        // The below variable is temporary and only for getting the program to execute.
        boolean temp = false;
        return temp;
    }

    public boolean isGregorian(){
        // This function enforces the Gregorian requirement for the due date.
        return false;
    }
}
